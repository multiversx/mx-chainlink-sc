/*
pub trait AggregatorInterface {
    fn decimals(&self) -> u8;
    fn description(&self) -> BoxedBytes;
    fn version(&self) -> u64;

    fn get_round_data(&self, round_id: u64) -> SCResult<RoundData<BigUint>>;
    fn latest_round_data(&self) -> SCResult<RoundData<BigUint>>;
}
*/
